server:
  host: "0.0.0.0"
  port: 8080

backends:
  codebase:
    host: "localhost"
    port: 9001
  database:
    host: "localhost"
    port: 9002
  ai:
    host: "localhost"
    port: 50051
    # Use base_url to call the Python AI service via its HTTP wrapper (RAG/Chat).
    # Run: cd backend/services/ai-service && python main.py && python scripts/serve_web_test.py
    base_url: "http://localhost:8888"
  local_agent:
    host: "localhost"
    port: 50051

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0